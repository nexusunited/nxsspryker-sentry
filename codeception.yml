namespace: NxsSprykerTest

paths:
    tests: tests
    support: .
    log: tests/_output
    data: tests/_data
    envs: tests/_envs

coverage:
    enabled: true
    whitelist: { include: ['src/*'] }
